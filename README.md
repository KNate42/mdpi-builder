# MDPI Builder

Manuscript editor with a live MDPI-style preview.

## Local run

```bash
npm install
npm start
```

The app runs at `http://localhost:3000`.

## GitHub Pages

The Pages workflow publishes the static files from `public/`.
